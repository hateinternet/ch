{
  "private": true,
  "devDependencies": {
    "csscomb": "4.2.0",
    "gulp": "3.9.1",
    "gulp-autoprefixer": "3.1.1",
    "gulp-concat": "2.6.1",
    "gulp-csso": "3.0.0",
    "gulp-htmlmin": "3.0.0",
    "gulp-uglify": "3.0.0"
  },
  "scripts": {
    "build": "npm run dev && gulp build",
    "csscomb": "csscomb src/css",
    "dev": "npm run csscomb && gulp dev",
    "init": "(gulp -v || npm install --global gulp-cli) && npm i",
    "watch": "npm run dev && gulp watch"
  }
}
